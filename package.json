{
  "name": "battlefy-lolstats-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "client-install": "cd client && yarn",
    "client": "cd client && yarn start",
    "start": "node index.js",
    "bin-start": "node ./bin/www",
    "server": "nodemon index.js",
    "dev": "concurrently \"node index.js\" \"yarn client\"",
    "dev-build": "concurrently \"yarn server\" \"yarn heroku-postbuild\"",
    "build": "cd client && yarn && yarn build",
    "heroku-postbuild": "cd client && yarn && yarn build"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "concurrently": "^5.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "fs": "^0.0.1-security",
    "got": "^11.3.0",
    "nodemon": "^2.0.4"
  },
  "devDependencies": {}
}
